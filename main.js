//create a function that return the module of the division and not the integer
function divisionRemainder(a , b) {
    var divide = a % b;
    return divide;
  }
  console.log(divisionRemainder(13 , 2));